/////////////////////////////////////////////////////////////////
/////////////////////////// REDUCERS ////////////////////////////
/////////////////////////////////////////////////////////////////
export interface RootReducerInterface {
	bookReducer: BookReducerInterface
}

export interface BookReducerInterface {
	searchTerm: string
	selectedBook: Book
}

/////////////////////////////////////////////////////////////////
//////////////////////// ACTION CREATORS ////////////////////////
/////////////////////////////////////////////////////////////////
export type SearchByTermAction = (searchTerm: string) => { type: string; payload: string }
export type OpenBookDialogAction = (book: Book) => { type: string; payload: Book }
export type CloseBookDialogAction = () => { type: string }
/////////////////////////////////////////////////////////////////
/////////////////////////// MODEL ///////////////////////////////
/////////////////////////////////////////////////////////////////
export interface Book {
	title: string
	isbn: string
	author: string
	publisher: string
	year: string
	language: string
	weight: number
	size: {
		height: number
		width: number
		length: number
	}
	[key: string]: any
}
